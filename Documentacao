```mermaid
erDiagram

    USUARIOS ||--o{ APOSTAS : "realiza"

    CAVALOS ||--o{ INSCRICOES : "participa"

    JOQUEIS ||--o{ INSCRICOES : "monta"

    CORRIDAS ||--o{ INSCRICOES : "possui"

    INSCRICOES ||--o{ APOSTAS : "recebe"

    INSCRICOES ||--o| RESULTADOS : "gera resultado"

    USUARIOS {
        int id PK
        string nome
        string email UK
        string senha_hash
        enum perfil
        timestamp criado_em
    }

    CAVALOS {
        int id PK
        string nome
        string raca
        int idade
        decimal peso
    }

    JOQUEIS {
        int id PK
        string nome
        decimal peso
        int vitorias
    }

    CORRIDAS {
        int id PK
        string hipodromo
        datetime data_hora
        int distancia
        enum status
    }

    INSCRICOES {
        int id PK
        int corrida_id FK
        int cavalo_id FK
        int joquei_id FK
        decimal odd_inicial
    }

    APOSTAS {
        int id PK
        int usuario_id FK
        int inscricao_id FK
        decimal valor_apostado
        enum status_aposta
        timestamp criado_em
    }

    RESULTADOS {
        int id PK
        int inscricao_id FK UNIQUE
        int posicao
        string tempo_gasto
    }
```
