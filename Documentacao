```mermaid
erDiagram

    USUARIOS {
        INT id PK
        VARCHAR nome
        VARCHAR email UK
        VARCHAR senha_hash
        ENUM perfil
        TIMESTAMP criado_em
    }

    CAVALOS {
        INT id PK
        VARCHAR nome
        VARCHAR raca
        INT idade
        DECIMAL peso
    }

    JOQUEIS {
        INT id PK
        VARCHAR nome
        DECIMAL peso
        INT vitorias
    }

    CORRIDAS {
        INT id PK
        VARCHAR hipodromo
        DATETIME data_hora
        INT distancia
        ENUM status
    }

    INSCRICOES {
        INT id PK
        INT corrida_id FK
        INT cavalo_id FK
        INT joquei_id FK
        DECIMAL odd_inicial
    }

    APOSTAS {
        INT id PK
        INT usuario_id FK
        INT inscricao_id FK
        DECIMAL valor_apostado
        ENUM status_aposta
        TIMESTAMP criado_em
    }

    RESULTADOS {
        INT id PK
        INT inscricao_id FK UK
        INT posicao
        VARCHAR tempo_gasto
    }

    USUARIOS ||--o{ APOSTAS : realiza

    CAVALOS ||--o{ INSCRICOES : participa

    JOQUEIS ||--o{ INSCRICOES : monta

    CORRIDAS ||--o{ INSCRICOES : possui

    INSCRICOES ||--o{ APOSTAS : recebe

    INSCRICOES ||--o| RESULTADOS : gera
```
